# Comp484_Hw9
Date: GetMonth(), getDate(), getYear()
Number: Number(), Number.isNaN(), Number.isInteger(), toFixed()
DOM: get ElementById(), textContent, innerHTML

![Webpage Screenshot](./Screenshot.png)


I had no trouble doing this homework page. The entire homework was pretty easy. One thing i learned from 'Date' was that months are zero indexed, thus Jan is 0. Another thing I learned is that the 'Number' object can validate data types using isNaN and isInteger. Lastly I learned how to dynamically update the DOM to display calculation results in the browser.
